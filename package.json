{
  "name": "esphome_ntchrono",
  "version": "1.0.0",
  "description": "Night Table Chrono - Smart Alarm and Clock for my night table",
  "main": "./dist/generate_glyps.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "font_x": "svgtofont --sources ./src/icon --output ./dist/font --fontName glyps-font --startUnicode 0x16",
    "font": "npm run clean && node src/generate_glyps.mjs",
    "build": "tsc",
    "watch": "tsc -w ",
    "dev": "tsc -w & nodemon dist/main.js",
    "clean": "rimraf dist"
  },
  "author": "@dianlight",
  "license": "Apache-2.0",
  "dependencies": {
    "rimraf": "^6.0.1",
    "svgtofont": "^6.0.0",
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
